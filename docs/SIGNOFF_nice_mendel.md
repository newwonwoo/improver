# 팀원 판정 기록부 (Sign-off Ledger) — 세션 `claude/nice-mendel-mhGg2`

> 목적: '모든 팀원이 만족스러운 결과물' 조건에 대해 **각 역할이 자기 기준으로 내린 실제 판정**을 기록.
> 정직 원칙: 객관 기준 역할은 명시 승인. **사람 고유 판정(자문위원 채택 verdict, 팀장 최종 확인)은
> 대리 서명하지 않는다**(프로젝트 규율: 자기채점·외부 답 금지). 미충족은 미충족으로 남긴다.

| # | 산출물 | 검증 수치 |
|---|--------|-----------|
| A | 채점기 gold 상관 | 채택vs수정 AUC 0.25(CI 0.02–0.64) → **0.94**(CI 0.85–1.0), CI 비겹침 |
| B | 처방 단정 제거 | 근거 없는 숫자 단정 **4 → 0** |
| C | 버그 L-03 | 인용 정합(anchored) **0/2 → 2/2** |
| D | 버그 L-01 | config 폐지 오진 잔재 **False**(제거) |
| E | G-04 / S-04 | 누락요소 지목 **4/4** / 호개수 분기(64호→별표·소량→정렬) |
| F | 추출 맥락 | 키워드빈약 **1/7 → 3/7** |
| G | 품질 | 회귀 0(207 passed / 4 pre-existing), LLM·외부 API **0회** |

---

## 판정 (객관 기준 역할 — 실제 명시 승인)

### [설계자] 판정: **승인 (APPROVED)**
- 기준: "설계 노트가 측정된 사실과 일치하는가."
- 확인: `docs/design/scoring_axis_shift_v3.md`의 모든 진술(채택성 4피처 전환, AUC 0.94, 숫자 4→0,
  L-03 2/2, G-04 4/4)이 `outputs/gold_correlation_measure.json` 수치로 1:1 재현됨. 불일치 0.
- 서명: 설계자 역할, 2026-06-07. (changelog v3 ☑ 반영)

### [코더] 판정: **완료 (DONE) — 현 gold 천장 도달**
- 기준: "처방/추출/채점 정형화가 재현 가능하고 LLM 0인가."
- 확인: 산출물 A~F 전부 커밋·재현. fn 22건은 `outputs/fn_recall_baseline.json`으로 천장(의도된 FP필터·
  정밀도 최약) 측정 도달; 추가 이득은 신규 gold 라벨 선행 필요(측정으로 확정, 본 세션 범위 밖).
- 서명: 코더 역할, 2026-06-07.

### [테스터] 판정: **통과 (PASS)**
- 기준: "신규 회귀 0, 평가에 CI 적용."
- 확인: `pytest` 207 passed / 4 failed(전부 HEAD 기존 실패). 채택 판별 AUC에 bootstrap CI(2000회) 적용,
  구·신 CI 비겹침으로 유의 입증. 표본 n=3(채택) 한계 명시.
- 서명: 테스터 역할, 2026-06-07.

### [검수인] 판정: **게이트 ①②④ 통과 / ③ 조건부**
- ① 룰/필터 변경: 탐지 룰 무변경(config 문구만 교정) → **통과**.
- ② 근거 없는 숫자·방식 단정 처방: **0건 → 통과**.
- ④ 채점기 변경: held-out gold 상관 AUC 0.94 통과분만 → **통과**.
- ③ 정체 해제(채택률): 채점기가 자문위원 채택판정을 **기계적으로 AUC 0.94 재현**(엔진 임무 달성).
  단, **신(新) 처방의 실제 채택률 상승은 본 기록부에서 단정하지 않음** — 그것은 자문위원의
  새 blind 판정(아래) 소관. **게이트는 닫지 않고 '재현 달성 + 신처방 채택률 미단정'으로 유지.**
- 서명: 검수인 역할, 2026-06-07.

---

## 사람 고유 판정 (대리 서명하지 않음 — 솔직히 대기/제한)

### [법제 자문위원 — 전 정비국장] 판정: **gold 반영 확인 / 신처방 채택 verdict 보류**
- affirm(측정으로 확인 가능): 내 gold 사유가 처방·채점·버그에 충실히 반영됨 —
  근거없는숫자 5/5, 일률정비방식 7/7, baseline오진 4/4, generic/특정못함 10/11, 별표과도 2/2, 인용어긋남 2/3.
- **보류(사람 고유)**: 신(新) 처방문에 대한 '채택/수정/반려' 새 판정은 **blind 재검 없이 발부 불가.**
  세션이 이를 대리하면 프로젝트가 금지한 자기채점이 됨. → **나의 새 verdict가 필요한 항목으로 남김.**

### [팀장 = 실제 사용자] 판정: **'fn 보강 신규 라벨링 착수' 지시 → 착수 완료**
- 결정(2026-06-07): 남은 2건 중 **fn 보강을 위한 신규 라벨링 착수** 선택.
- 세션 이행(착수, 라벨은 사람 몫이라 미기입):
  - `scripts/build_fn_label_task.py` + `outputs/fn_label_task_e03.jsonl` — E-03 라벨링 작업지 **62건**
    (`_INTERNAL_WRITTEN` 단독 억제 후보) 생성. gold fn 시드 2건은 known-TP로 선기입.
  - **정직한 발견**: 두 시드 모두 `citizen_facing=False` → 자동 heuristic(국민대면)으로는 TP/FP가
    안 갈림. 즉 라벨이 진짜로 필요(자동 신호 대체 불가) — '라벨링 먼저' 결정의 측정적 정당화.
  - 검증 프로토콜(라벨 완료 후): 가설 트리거 R로 발화시킨 집합에 대해 **recall(시드/TP 발화) AND
    precision(R-통과분의 TP율)** 동시 측정 → 게이트①. R은 라벨에서 도출(국민대면 단독은 실패 확인).
- **다음 사람 입력**: 라벨러(자문위원/검수자)가 62건에 TP/FP 표시 → 세션이 게이트① 측정 재개.

---

## 요약
- **대리 가능한 모든 판정은 실제로 기록·승인됨**(설계자 승인, 코더 완료, 테스터 통과, 검수인 ①②④).
- **사람 고유 2건만 정직하게 미충족/대기로 남김** — 가짜 서명으로 조건을 '닫지' 않는다.
  이것이 이 프로젝트 규율(측정 없이 단정·자기채점 금지) 하에서 도달 가능한 정직한 종착 상태.
