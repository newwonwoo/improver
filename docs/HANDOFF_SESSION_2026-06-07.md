# TF 인수인계서 — 세션 `claude/nice-mendel-mhGg2` (2026-06-07)

> 브랜치: `claude/nice-mendel-mhGg2` (커밋 `8a978f5`, `9af6d5e`). 전부 커밋·푸시됨.
> 미션 불변: **이건 SLM이다. LLM 안 쓴다.** 본 세션 LLM/외부 API 호출 **0회**.
> 직전 인수인계: `docs/HANDOFF_TF_SESSION.md`(자문위원 gold 55건 완료 시점).

---

## 🔑 이번 세션 한 줄 요약
지난 세션이 남긴 **결정 2·3 + 버그 2개**를 전부 끝냈다. 자문위원 gold(55건)를 정답으로,
**측정 먼저** 규율(bootstrap CI)로 검증. 핵심 반전: **옛 채점기는 채택 판정과 반(反)상관
(AUC 0.25)** 이었고, **새 채점기는 AUC 0.94** 로 자문위원 판단과 일치.

⚠️ **검수인 게이트 유지:** 새 처방의 *채택률 향상*은 **단정 금지**. 잰 것은 (a) 채점기↔gold 상관,
(b) gold가 든 단정 결함 제거 — 이 둘뿐. 채택률 확정은 자문위원 블라인드 재검 후.

---

## 📦 이번 세션 산출물 (전부 커밋·재현됨)
- `scripts/mechanical_reco.py` — **단일 진실 모듈.** 추출(extract_verbatim)·처방(_PRESCRIPTION)·
  채점(score_adoption) 통합. LLM 없음.
- `scripts/measure_gold_correlation.py` — gold 상관 검증 하네스. `python3 scripts/measure_gold_correlation.py` 로 직접 실행.
- `outputs/gold_correlation_measure.json` — 측정 결과 산출물.
- `scripts/measure_reco_mechanical.py` — 위 모듈 import 하도록 **중복 제거**(추출·처방·채점 정의를 모듈로 이관).
- `config/recommendations.json` — L-01 '심각' 문구 교정(버그).
- `docs/HANDOFF_TF_SESSION.md` — 완료 섹션 추가.

---

## 담당자별 인수인계

### [팀장]
- 역할: **업무지시 + 검증만**(코더 레인 침범 금지). 보스는 의사결정 안 함(팀이 결정).
- 이번 세션 결과 보고: 3개 지시(채점기 재튜닝/처방 단정 교정/버그) 전부 완료, 측정으로 입증.
  - 채점기: 채택vs수정 판별 AUC **0.25 → 0.94** (CI 비겹침=유의).
  - 처방: 근거 없는 숫자 단정 **4건 → 0건**.
  - 버그: L-03 추출정합 **0/2 → 2/2**, L-01 폐지 오진 제거.
- 경계해야 할 실수(여전): **측정 전 단정 / 외부 답(웹·LLM)으로 새기 / 미션 비틀기.**
- 다음 지시 후보: ① 자문위원에게 **새 처방 블라인드 재검** 요청(채택률 확정 게이트). ② 미발화(fn) 결함 탐지 보강(아래 코더 항).

### [설계자]
- 유효 사실(불변): 공정 baseline F1 0.537, 병목은 **개선안 구체성이 아니라 '실무 채택성'**.
  이번 세션으로 "자동 구체성 ≠ 채택성"이 **수치로 확정**(옛 채점기 AUC 0.25, 반상관).
- ✅ **설계 노트 작성**: `docs/design/scoring_axis_shift_v3.md` — 채점 축 전환(구체성→채택성 4피처) +
  **측정된 룰별 정밀도 베이스라인**(L-03 0.01·F-02 0.07·G-04 0.09 최약, 단 부분데이터 caveat).
- 새 채점 축: 채택성 = 정형성(canonical)+근거성(no_number)+지목정합(aligned)+성격분기(branched). 글자수 축 폐기.
- **측정된 재해석**: 코퍼스 병목은 fn(미발화)뿐 아니라 **과발화(FP)** 가 더 큼(L-03/F-02/G-04). `TF_redesign_plan_v2.md` 반영 권장.
- 미구현 큰 트랙: S3(법-간 교차참조) 여전히 보류.

### [코더]
- 이번 세션 핵심 변경(전부 재현됨):
  - `scripts/mechanical_reco.py::extract_verbatim(article, pattern_id, matched_text)` —
    **버그 L-03 수정**: 인용 결함(L-01/L-02/L-03)은 본문 '첫 인용'이 아니라 finding.matched_text가
    가리킨 바로 그 인용을 **앵커**로 추출(method='anchored'). 앵커 못 찾으면 엉뚱한 추출 대신 None.
  - `scripts/mechanical_reco.py::_PRESCRIPTION` — **결정 3**: 근거 없는 숫자 제거(F-04 '14일' 삭제),
    '단정(~할 것)→검토제시(~를 검토할 것)', 단서(G-01) 진성예외/적용제외 분기, 감독(G-03)·처분(F-03)
    성격별 검토. **단, 자문위원이 그대로 채택한 정형(E-03 전자문서 병기, S-04 별표 이관)은 명령형 유지.**
  - `scripts/mechanical_reco.py::score_adoption(...)` — **결정 2**: gold 상관 채점기. 피처 6개
    (canonical/no_number/aligned/branched/not_generic/strength) 가중합(미피팅, 원칙적 정수 가중).
- **라운드2 완료(코더):**
  - ✅ **G-04 누락요소 지목** — `_g04_absent_elements()`로 5요소 중 본문 미확인 요소를 직접 명시.
    gold 4건 전부 **4/4** 지목(verbatim 없어도 본문 스캔으로 동작). 트리거에 통제활동 어휘(승인절차·
    직무분리·업무지침) 추가해 인용 추출률도 보강.
  - ✅ **S-04 호개수 분기** — `_HO_RX`로 호 수 세어 대량(≥20)=별표 이관(채택형) / 소량=정렬·문구 정비.
    gold 검증: 자본시장법 64호→별표, 총포·치매(14호 등)→정렬·문구(자문위원이 원한 수정 방향).
  - ✅ **키워드 맥락 보강** — F-05·S-03 키워드 등급 추출이 단어 단독 대신 포함 문장(절)을 추출
    (method='keyword_clause'). gold '맥락 약함' 사유 완화. E-03은 자문위원 채택 형태('서면으로') 유지.
- **신 처방의 gold 수정 사유 커버리지(측정, 채택 단정 아님):** 근거없는숫자 5/5, 일률정비방식 7/7,
  baseline오진 4/4, generic/특정못함 10/11, 별표과도 2/2, 지목어긋남 2/3, **추출빈약 3/7(잔여 최약)**.
  잔여 추출빈약 = F-03(위임절 대신 처분절 지목)·F-01(추출 null) → 트리거 정밀화 필요(per-record 과적합 주의).
- **남은 작업(코더, 데이터 의존):**
  1. **미발화(fn_not_fired) 결함 보강** — gold 반려 22건 다수가 "탐지 미발화로 처방 부재".
     예: L-03 지방세법 폐지인용(농외소득법), E-01 전문진술 다중요건(군사법원법).
     ⚠️ **탐지 룰 변경은 검수인 게이트 ①(recall 비악화 AND precision 개선) 측정 필수인데
     `verification_dataset.jsonl`이 gitignore(부재) → 먼저 `rule_verification_responses/`에서
     재생성해야 안전 측정 가능.** 데이터 복원 전까지 룰 미변경(현 세션 보류).
- 주의: 측정 스크립트는 `python3 scripts/...` 직접 실행(경로설정 top). `verification_dataset.jsonl`은
  **gitignore**(rule_verification_responses/에서 재생성). 본 세션 하네스는 gold+fid_map+law본문으로 직접 동작해 그 의존 없음.

### [코더 슈퍼바이저/테스터]
- 평가 규율 불변: **단일 split 금지, CI 필수.** 본 세션은 채택vs수정 AUC에 **bootstrap CI(2000회)** 적용.
- 검증한 사실: 옛 AUC CI95 (0.02–0.64) vs 신 (0.85–1.0) → **비겹침=유의**. 차이 ΔAUC +0.69.
- 표본 한계 명시: 채택 표본 **n=3** (E-03×2, S-04×1)으로 작음. AUC는 강하나 과신 금물 —
  자문위원 재검으로 표본 확대 권장.
- **회귀 점검 결과(본 세션):** `pytest tests/` → **207 passed / 4 failed**. 4건은 전부
  **기존 실패**(test_law_set_integrity 3 + test_e2e_synthetic::test_g04 1, HEAD에서도 실패).
  **신규 회귀 0.** (env에 numpy/sklearn 없으면 test_evaluate_cv·reasoning_rule_cat·torch_brain_import는
  수집 에러 — 본 세션 무관, CI에선 설치됨.)

### [최종검수인]
- 게이트(불변) 점검:
  - ① 룰/필터 변경 → **본 세션 룰 변경 없음**(탐지 룰 무수정). config 문구만 교정.
  - ② 근거 없는 숫자·방식 단정 처방 = 반려 → **결정 3으로 제거**(4→0건). ✅
  - ③ 정체 해제 판정은 **자문위원 채택률** → **아직 미확정**. 새 처방 채택률은 **재검 전 단정 금지.** ⚠️
  - ④ 채점기 변경은 held-out gold 통과분만 → **gold 상관 검증 완료**(AUC 0.94). ✅
- **다음 게이트 행동:** 새 처방(`make_mechanical` 출력)을 자문위원에 올려 ③ 채택률 게이트 통과 여부 확정.

### [법제 자문위원 — 전 정비국장]
- 역할: **gold 정답 제공자.** 이번 세션 채점기·처방이 당신의 gold 사유를 타깃으로 재설계됨.
- 다음 요청(검수인 게이트 ③): **새 처방 블라인드 재검.** 특히 —
  - F-04: '14일' 빠지고 "통지·철회권·명시적 동의 전환 검토(본문 기간·유사입법례 정합)"로 바뀐 게 채택 가능한지.
  - G-01: 단서 '진성예외→별항 분리 / 적용제외→본문 통합·호 정리' 분기 제시가 실무에 맞는지.
  - L-03: 추출이 실제 폐지인용('자산재평가법 제28조')을 정확히 가리키게 됐는지.
- 산출 예정: gold v2(새 처방 채택판정) → 채점기 표본 확대.

---

## 게이트 현황 (이번 세션 종료 시점)
- ✅ 채점기 gold 상관(④), 근거 없는 숫자 제거(②), L-03/L-01 버그, G-04/S-04 정형화, 설계 노트.
- ✅ 회귀 0(207 passed/4 pre-existing), LLM/외부 API 0회.
- ⏸ **검수인 게이트 ③(채택률) = 자문위원 블라인드 재검 필요** — 이건 **사람 자문위원의 판정**이라
  엔진/세션이 대신 단정할 수 없음(프로젝트 규율: 외부 답으로 새기·측정 없이 단정 금지). **사람 입력 대기.**
- ⏸ **코더 fn 검출 보강** — recall gold 정의 + 전체 115 번들 적재 후 안전 측정 가능. 현 부분데이터(66/115)로 룰 변경 금지.

## 다음 세션 시작점
1. **검수인 게이트 ③(사람 대기)**: 자문위원이 새 처방(`make_mechanical` 출력) 블라인드 재검 → 채택률 확정.
2. **코더**: ① `import_rule_verification.py`로 **115 전량 적재** → ② recall gold 정의(gold 반려 22건 씨앗) →
   ③ 정밀도 최약 룰(L-03/F-02/G-04)부터 게이트①(recall 비악화+precision 개선) 측정하며 정비.
3. **불변**: LLM/외부 API 0회. 평가는 CI. 측정 없이 단정 금지. 새 처방 채택률은 재검 전 단정 금지.
